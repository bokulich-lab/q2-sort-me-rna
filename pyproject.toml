[project]
name = "q2-sort-me-rna"
authors = [
    { name = "Thomas Creavin", email = "thomas.creavin@gmail.com" }
]
description = "Filter and align ribosomal RNA reads with SortMeRNA in QIIME 2."
readme = { file = "README.md", content-type = "text/markdown" }
license = { file = "LICENSE" }
dependencies = [
    "pandas"
]
dynamic = ["version"]

[project.urls]
Homepage = "https://github.com/bokulich-lab/q2-sort-me-rna"
Repository = "https://github.com/bokulich-lab/q2-sort-me-rna"

[project.entry-points."qiime2.plugins"]
"q2-sort-me-rna" = "q2_sort_me_rna.plugin_setup:plugin"

[build-system]
requires = [
    "setuptools",
    "versioningit",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.versioningit.vcs]
method = "git-archive"
describe-subst = "$Format:%(describe)$"
default-tag = "0.0.1"

[tool.versioningit.next-version]
method = "minor"

[tool.versioningit.format]
distance = "{base_version}+{distance}.{vcs}{rev}"
dirty = "{base_version}+{distance}.{vcs}{rev}.dirty"
distance-dirty = "{base_version}+{distance}.{vcs}{rev}.dirty"

[tool.versioningit.write]
file = "q2_sort_me_rna/_version.py"

[tool.versioningit]
default-version = "0.0.dev0"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["q2_sort_me_rna*"]

[tool.setuptools.package-data]
q2_sort_me_rna = [
    "citations.bib",
    "tests/data/*",
    "tests/data/seq/*",
    "tests/data/sortmerna-output/*"
]
